type OwnProps = {
  // videoId: string;
  // sign: string; // 처음, 중간, 끝
  // level: string; // easy, normal, hard
  handleClick: (e: any) => void;
};

export const YouTubePlayer = (props: OwnProps) => {
  const { handleClick } = props;

  return <div />;
};
