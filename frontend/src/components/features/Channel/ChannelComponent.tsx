import React from 'react';
import { ChannelItemsWrapper, ChannelItem } from './ChannelComponent.styled';

export const ChannelComponent = () => (
  <ChannelItemsWrapper>
    <ChannelItem>이것은 채널이다</ChannelItem>
  </ChannelItemsWrapper>
);
